 DemoGridLongCell connected;F;values;cellTypes;rowTitles;colTitles;n;longText
 EWC.CONNECTED←connected
 ⎕PW←1000

⍝ Reported issue: in a Grid with a large dataset where a cell's content
⍝ is too long to fit, clicking on an editable cell shows the blue selection
⍝ outline only spanning roughly half the cell width.
⍝ This demo exercises both conditions (many rows + long values + wide cells)
⍝ across several layouts so we can see whether the selection border really
⍝ underspans the cell.

 F←'F1'
 F eWC'Form' 'Grid Long-Cell Selection Repro'(50 50)(900 1500)('Coord' 'Pixel')

 n←60
 longText←'A very long product description that does not fit inside the visible portion of the cell at all'

⍝ ─── Grid A: large dataset, wide cells, very long text ───────────────────
⍝ Hypothesis: with cell content wider than the visible cell width and many
⍝ rows scrolling vertically, the blue selection border may render only on
⍝ the visible portion (left half) of the cell.
 (F,'.LA')eWC'Label' 'A: 60 rows, wide cells (300px), long text values'(60 30)(24 700)
 (F,'.GA')eWC'Grid'('Posn'(90 30))('Size'(260 700))('CellWidths'(300 300 100))('CurCell'(1 1))('VScroll' ¯1)('HScroll' ¯1)
 valuesA←n 3⍴⊂''
 valuesA[;1]←{longText,' #',⍕⍵}¨⍳n              ⍝ long text per row
 valuesA[;2]←{'Short ',⍕⍵}¨⍳n                   ⍝ short text
 valuesA[;3]←⍳n                                  ⍝ numeric
 (F,'.GA')eWS('Values' valuesA)('CellTypes'(n 3⍴1 1 2))
 (F,'.GA')eWS'ColTitles'('Long Description' 'Short' 'Qty')
 (F,'.GA')eWS'RowTitles'(⍳n)
 (F,'.GA.E1')eWC'Edit'
 (F,'.GA.E2')eWC'Edit'('FieldType' 'Numeric')
 (F,'.GA')eWS'Input'((F,'.GA.E1')(F,'.GA.E2'))

⍝ ─── Grid B: same data as A but smaller cell width (60 px) ───────────────
⍝ Cells too narrow to fit any of the text content; ellipsis kicks in.
⍝ Click a cell to see whether the selection outline still spans the full
⍝ visible td width or only part of it.
 (F,'.LB')eWC'Label' 'B: same data, narrow cells (60px) — ellipsis everywhere'(440 30)(24 700)
 (F,'.GB')eWC'Grid'('Posn'(470 30))('Size'(260 283))('CellWidths'(60 60 60))('CurCell'(1 1))('VScroll' ¯1)('HScroll' ¯1)
 (F,'.GB')eWS('Values' valuesA)('CellTypes'(n 3⍴1 1 2))
 (F,'.GB')eWS'ColTitles'('Long Description' 'Short' 'Qty')
 (F,'.GB')eWS'RowTitles'(⍳n)
 (F,'.GB.E1')eWC'Edit'
 (F,'.GB.E2')eWC'Edit'('FieldType' 'Numeric')
 (F,'.GB')eWS'Input'((F,'.GB.E1')(F,'.GB.E2'))

⍝ ─── Grid C: mixed widths to bracket the boundary ────────────────────────
⍝ One narrow column next to one wide column, varying long content.
⍝ Placed in its own column on the right with extra padding.
 (F,'.LC')eWC'Label' 'C: mixed widths (50 / 350 / 80) — click each col to compare'(60 800)(24 600)
 (F,'.GC')eWC'Grid'('Posn'(90 800))('Size'(342 583))('CellWidths'(50 350 80))('CurCell'(1 1))('VScroll' ¯1)('HScroll' ¯1)
 valuesC←20 3⍴⊂''
 valuesC[;1]←⍳20
 valuesC[;2]←{longText,' row=',⍕⍵}¨⍳20
 valuesC[;3]←⍳20
 (F,'.GC')eWS('Values' valuesC)('CellTypes'(20 3⍴2 1 2))
 (F,'.GC')eWS'ColTitles'('#' 'Long Description' 'Qty')
 (F,'.GC')eWS'RowTitles'(⍳20)
 (F,'.GC.E1')eWC'Edit'
 (F,'.GC.E2')eWC'Edit'('FieldType' 'Numeric')
 (F,'.GC')eWS'Input'((F,'.GC.E1')(F,'.GC.E2'))
