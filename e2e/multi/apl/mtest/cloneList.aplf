 r←cloneList arg;n;ix
⍝ Space-separated names of the live per-session clones, sorted by their
⍝ numeric suffix

 n←{⍵~' '}¨↓#.⎕NL 9
 n←n/⍨{'mtest_'≡6↑⍵}¨n

 :If 0=≢n
     r←'(none)'
 :Else
     ix←{⊃2⊃⎕VFI 6↓⍵}¨n
     n←n[⍋ix]
     r←1↓∊' ',¨n
 :EndIf
