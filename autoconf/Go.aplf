 Go x
⍝ Test driver: (re)build the AutoConf form and pump events from WITHIN #.autoconf
⍝ so EWC's findCaller resolves the caller namespace (bare eDQ from a REPL can't).
⍝ Build is error-trapped so it is a no-op if the objects already exist. Call: Go '.'
 AC_RENDER←'EWC'
 {0::0 ⋄ BuildAutoConf}0
 eDQ x
